{
  "name": "fixlap-monorepo",
  "version": "1.0.0",
  "description": "FixLab - Responsive Device Repair Management (React Frontend + Laravel Backend)",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --host 0.0.0.0",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview -- --host 0.0.0.0",
    "server": "php -S 0.0.0.0:80 -t backend/public",
    "backend": "cd backend && php artisan serve --host=0.0.0.0 --port=80",
    "artisan": "php backend/artisan",
    "deploy": "bash deploy.sh"
  }
}
